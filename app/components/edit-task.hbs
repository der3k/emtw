{{#let
  (hash
    AddComment=(component
      "add-comment"
      onSubmit=this.addComment
      onClose=(fn (mut this.show.addComment) false)
    )
    show=this.show
  )
  as |command|
}}
  {{yield command}}
{{/let}}
