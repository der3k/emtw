{{#let
  (hash
    AddComment=(component
      "add-comment"
      onSubmit=this.addComment
      onClose=(fn (mut this.showAddComment) false)
    )
    showAddComment=this.showAddComment
  )
  as |command|
}}
  {{yield command}}
{{/let}}
